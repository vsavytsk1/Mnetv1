# the loose net/ folder is scratch from running scripts by hand (regenerable).
# the REAL, kept artifacts live under builds/vNNN/ (the version journey + vault).
/net/

# screenshots from ad-hoc console runs (builds/ keeps its own thumb.png)
neo_console_shot.png
*_boot.png
