v009 -- BROKEN / PARTIAL (kept, never deleted -- "we never delete, all logged")

WHAT HAPPENED
  This was a genesis level-9 build (max=9, K=4, bits=10101), the deep milestone.
  The ~11-minute CPU join grind was running in the background and got cut off
  mid-level-9: genesis L0-9 and joins up to L8 completed, and join_L9.i32 (the
  wire file) was written, but the run stopped BEFORE:
    - join_L9.json  (the L9 join manifest)
    - the gate pass  (04_gate)
    - the vault      (redundancy 3-format + SHA-256)
    - build_card.json
  No python process remained. This is a partial, like a geological layer.

WHY IT IS KEPT
  Axiom 10 / the genizah law (TITANS.md): we never delete, all is logged. A
  broken day is still a day. It stays as an honest record of the interruption,
  not scrubbed from the journey. It is simply NOT a valid build: no vault, no
  card, do not trust its numbers, do not push it as a milestone.

THE CLEAN ONE
  L9 was rebuilt cleanly as the next version (see that build's build_card.json).
  Use that for anything real. This folder is the marker of the day it broke.

  -- logged 2026-07-08
