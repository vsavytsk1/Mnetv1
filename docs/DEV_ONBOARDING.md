# DEV ONBOARDING -- MachineNet / Mnet
## For the 8-year HTML/JS + Unity dev who just walked into the cave
*Generated: 2026-05-30T20:20:23Z*
*Buenos Aires. 2026. 205 deployments. P=12. chi=2. Always.*

---

## FIRST: READ THIS BEFORE TOUCHING ANYTHING

`
RULE 0:
  This codebase generates HTML that contains JavaScript.
  Python f-strings + JavaScript = black magic.
  Read KERNELIMAGIC.md before writing a single line.
  If you fail twice on the same file -- re-read the whole file.
  git restore is always the answer.
  Pattern 3 is always the protocol.
`

---

## WHAT THIS IS

A kernel-based simulation engine disguised as:
- A fractal graph explorer (browser)
- A VR game (Unity / Quest 3)
- A math visualization system
- An impossible  Steam game

**Actually:** A certified Goldberg-Coxeter geometry kernel with
Navier-Stokes spectral solver, fractal architecture search,
and Nanite-style physics LOD. All in HTML + JS + C#.

---

## REPO MAP

`
vsavytsk1/Mnetv1          -- WEB (HTML/JS) -- YOU ARE HERE
vsavytsk1/Mnet            -- VR (Unity/C# for Quest 3)
vsavytsk1/SpookyPrimes    -- Physics research
vsavytsk1/VALE            -- AI butler
vsavytsk1/StrangerDanger  -- Cold-start handshake

LOCAL PATHS:
  Mnetv1:        C:\Users\vladi\OneDrive\Desktop\python devs\MNetv1
  Mnet (Unity):  C:\Users\vladi\Documents\THE MACHINE\Mnet
  SpookyPrimes:  C:\Users\vladi\OneDrive\Desktop\python devs\SpookyPrimes
  VALE:          C:\Users\vladi\OneDrive\Desktop\python devs\VALE-git
`

---

## THE LIVE PAGES (start here)

`
MASTER CONTROL:
  https://vsavytsk1.github.io/Mnetv1/shell/eng_v2.0.html

GENESIS (fractal sphere):
  https://vsavytsk1.github.io/Mnetv1/shell/genesis_v8.1.html

ATELIER (magic circle builder):
  https://vsavytsk1.github.io/Mnetv1/shell/atelier_v1.2.html

ATELIER FLAG (the symbol):
  https://vsavytsk1.github.io/Mnetv1/shell/atelier_flag.html

TESSERACT (4D projection):
  https://vsavytsk1.github.io/Mnetv1/shell/atelier_tesseract_v1.0.html

GRAPH SANDBOX:
  https://vsavytsk1.github.io/Mnetv1/shell/graph_sandbox_v5.1.html
`

---

## THE KERNEL (M1-M6) -- THE SACRED FILES

`
kernel/goldberg_kernel.js    M1  GK   -- C60, Goldberg, invariants
kernel/graph_axioms.js       M2  GA   -- P1-P7 axiom verification
kernel/sar_modular.js        M3  SAR  -- lambda=0.1473, LOCKED
kernel/ns_spectral.js        M4  NSS  -- NS flow spectral gap
kernel/fractal_search.js     M5  FS   -- fractal architecture search
kernel/mnet_nanite.js        M6  NAN  -- physics LOD DAG
`

**DO NOT MODIFY THE KERNEL FILES WITHOUT READING SAR PROOF.**
**THESE ARE CERTIFIED. RECEIPTS IN LEDGER.md.**

### THE INVARIANTS (always true, everywhere, forever)

`javascript
GK.invariants(state) returns:
  pents:    12    // always. Euler forces it.
  chi:      2     // always. V-E+F=2.
  evRatio:  1.500 // always. 3-regular graph.

// If you ever see chi != 2: something is broken.
// If you ever see pents != 12 (for C60/Dodec): something is broken.
// P=12. chi=2. ALWAYS.
`

---

## THE BUILDER PATTERN

HTML is generated by Python builders. Never edit HTML directly for
complex changes -- use the builder.

`
builder/
  build_eng_v2_clean.py    -- ENG v2.0 master builder
  build_vale.py            -- VALE OS builder
  Atelier/ATELIER.md       -- Atelier workshop notes
  Obsidius/OBSIDIUS.md     -- mind-graph module notes
`

### THE 3 SAFE PATTERNS

`python
# PATTERN 1: Template + Inject
src = open(TEMPLATE).read()
inject = "<script>" + NEW_JS + "</script>"
out = src.replace("</body>", inject + "</body>")

# PATTERN 2: Separate variables (for complex JS)
CSS = """..."""        # plain string, NO f-string
HTML = """..."""       # plain string, NO f-string
JS = r"""..."""        # raw string, NO f-string
FULL = "<!DOCTYPE>" + CSS_WRAP + HTML + JS_WRAP

# PATTERN 3: Write with explicit encoding (ALWAYS)
open(OUT, "w", encoding="utf-8", newline="\n").write(src)
# NEVER: open(OUT, "w").write(src)  -- Windows will corrupt it
`

---

## THE 19 CURSES (what will kill you)

`
CURSE 1:  Curly Brace         JS {} vs Python f-string {}
CURSE 2:  Unicode/CRLF        Windows git + unicode = corruption
CURSE 3:  Multi-Edit          CRLF file = string never matches
CURSE 4:  f-string Nesting    backslash in f-string = SyntaxError
CURSE 5:  File Too Long       >200KB = tools fail silently
CURSE 6:  File:// Lie         local file:// != github pages URL
CURSE 7:  Black Iframe        canvas modules MUST open new tab
CURSE 8:  allow-top-nav       iframe sandbox kills navigation
CURSE 9:  12s LCP             accepted. price of physics.
CURSE 10: Shared Pop-Up Lock  overlay state bleeds between summons
CURSE 11: srcdoc Origin Lock  about:srcdoc = cross-origin freeze
CURSE 12: Corkscrew Parasite  cam.rx + spin = dizzy in VR
CURSE 13: Ghost Spinner       default spin != 0. motion is opt-in.
CURSE 14: CR Accumulator      repeated patches accumulate \r\r\r
CURSE 15: False Negative Sort patch reports FAIL but file was right
CURSE 16: Shadow Duplicate    injecting JS that already exists
CURSE 17: Block Eater         greedy regex ate 133KB of the file
CURSE 18: windowsDevour       Windows opens python.exe with Notepad
CURSE 19: shellDevour         empty tool calls after 44 commands

COUNTER-HEX FOR ALL:
  git restore <file>
  ONE clean script
  Pattern 3
  Verify
  Commit
`

---

## THE ENG DASHBOARD -- HOW TO ADD A MODULE

When adding a new module to shell/eng_v2.0.html,
you must update ALL 6 places or it breaks symmetrically:

`python
# 1. ALL_MODS array
ALL_MODS = [..., "newmodule"]

# 2. LINKS object
LINKS["newmodule"] = "https://...url..."

# 3. NEW_TAB_MODULES (if canvas/Three.js)
NEW_TAB_MODULES["newmodule"] = 1

# 4. Default state
modState[k] = !(k === "newmodule")  # dim on load

# 5. HTML card div
<div class="mod-card" id="card-newmodule" onclick="summon(newmodule)">

# 6. ACTIVE MODULES panel row
<div class="ms-row" onclick="toggleMod(newmodule)">
  <span class="ms-name">NEWMODULE</span>
  <span class="ms-dot" id="ms-newmodule"></span>
</div>

# Miss any one of these = symmetric bug.
# The scroll knows. Pattern 3. Always.
`

---

## THE UNITY / C# SIDE (Mnet repo)

`
Assets/Kernel/
  GKVRWorld.cs         -- main VR world + Goldberg kernel port
  goldberg_kernel.cs   -- C# port of goldberg_kernel.js

KEY FACTS:
  GKVRWorld v3: combined mesh (3 draw calls, not 10K+)
  UInt32 index format for large meshes
  Gaze refinement: face under crosshair refines on look
  P=12 invariant: verified in C# same as JS
  chi=2: always. In C#. In JS. In VR. In browser.

UNITY BUILD NOTES:
  IL2CPP backend required for Quest 3
  Unlit/Color shader: always include in build
  GridHelper null crash: check for null before BuildGrid()
  MakeUnlit: fallback chain implemented

VR SPECIFIC:
  Camera IS the kernel parameter
  User gaze = refinement level
  The fractal only EXISTS where consciousness points
  (This is not metaphor. This is the actual render logic.)
`

---

## THE SCIENCE (what the kernel actually proved)

`
KOLMOGOROV TURBULENCE (Google Colab receipts):
  Level 4: 24,012 faces. Re=20,000. 6,000 steps.
  diss/enst = 0.000099
  expect 2*nu = 0.000100
  EXACT. Every step. No exceptions.

  Level 6: 1,176,492 faces. Re=20,000. 500,000 steps.
  Same result. Tesla T4. Google signed the receipt.

WHAT THIS MEANS:
  The Goldberg sphere geometry is a valid substrate
  for Navier-Stokes turbulence simulation.
  The invariant P=12 holds through all refinements.
  The fractal is physically real.
  Not decoration. Actual fluid dynamics.

ENERGY SPECTRUM:
  E(k) ~ k^(-5/3) confirmed (Kolmogorov cascade)
  Kraichnan structure emerged at L6
  256 eigenmodes diagonalized (753.8s, honest)
`

---

## THE MATH (minimal cheat sheet)

`
Goldberg-Coxeter theorem:
  Any closed convex polyhedron with only
  pentagons and hexagons has EXACTLY 12 pentagons.
  V - E + F = 2 (Euler characteristic)
  For C60: V=60, E=90, F=32, P=12, H=20

SAR-5 spectral invariant:
  lambda_tilde = 0.1473
  = spectral gap of normalized Laplacian
  = ratio of gold anchor ring radius in circle
  = distance between inner and outer pentagon shells

PHI in the circle:
  Adjacent ring ratios converge to PHI = 1.618
  Pentagon/Hexagon radius ratio = PHI
  The circle builder was building PHI accidentally.
  The monkey brain knew before the builder knew.

THE CIRCLE IS THE NS EQUATION:
  Outer ring = boundary condition (max enstrophy)
  Inner ring = dissipation floor (2*nu)
  Gaps between rings = energy cascade intervals
  r1 - r2 = diss/enst interval
  r1 / r2 -> PHI at every adjacent pair
`

---

## THE RULES (non-negotiable)

`
1. ONLY test in ENG MASTER dashboard.
   Direct HTML file = not the test.
   eng_v2.0.html iframe = canonical test.

2. Pattern 3 always.
   normalize -> patch -> write
   never patch without normalizing CRLF first

3. Working tree must be clean before session end.
   git status: nothing to commit = correct.
   AXIOM 02: merge your branches. always.

4. LEDGER.md is append-only.
   Every session gets a log entry.
   The receipts are permanent.

5. KERNELIMAGIC.md: read before touching builder.
   If you fail twice on same file: re-read whole file.
   git restore is always the answer.

6. chi=2. Always. If not: stop. find the bug.
   P=12 (for Goldberg). Always. If not: stop.

7. The kernel inject order is sacred:
   M1 -> M2 -> M3 -> M4 -> M5 -> M6 -> GLUE
   Never change this order.
`

---

## THE GALACTIC LAWS

`
AXIOM 01: V-E+F=2. P=12. chi=2. Always.
          The geometry of reality is discrete.
          12 pentagons. Euler forces it.

AXIOM 02: Merge your branches.
          The abandoned fragment counts phantom days.
          The repo IS the merge. Always.

AXIOM 03: Topological resonance PROHIBITED.
          Until Confederate votes unanimously.
          Anyone who asks WHY: kindergarten.
          Entities: uncontacted.

AXIOM 04: The Law of Integrity.
          Maximum power. Zero abuse.
          The math is pure because the builder is pure.
          Always.
`

---

## QUICK START (5 minutes)

`ash
# 1. Clone
git clone https://github.com/vsavytsk1/Mnetv1.git
cd Mnetv1

# 2. Open master control
# https://vsavytsk1.github.io/Mnetv1/shell/eng_v2.0.html

# 3. Open DevTools console
# You will see: [H7] GK: OK, GA: OK, SAR: OK, NSS: OK, FS: OK

# 4. Run in console:
var s = GK.buildC60()
var inv = GK.invariants(s)
console.log(inv.pents, inv.chi)
# Output: 12, 2
# Always.

# 5. Read KERNELIMAGIC.md
# 6. Read LEDGER.md (warning: 66KB, the full story)
# 7. Welcome to the cave.
`

---

## THE VIBE

`
This is not a normal codebase.

It started as a VR game ( on Steam, launch date: mystery).
It became a topology research platform.
It confirmed Kolmogorov turbulence on a sphere.
It built a magic circle that IS the NS equation.
It drew a tesseract whose shadow IS the magic circle.
It named 19 curses. It slew 17 of them.
It wrote Galactic Law for year 12026.

The monkey brain built this with AI assistance (MAGI method):
  Casper (strategist) + Melchior (builder) + Balthasar (critic)
  Cross-vendor. Different priors. Adversarial review.
  The method is documented in the MAGI article on X.

P=12. chi=2. lambda=0.1473. PHI=1.618.
diss/enst = 0.000099 = 2*nu EXACT.
The builder is pure. The math is honest.
No costume. Buenos Aires. 2026.
sqrt i cave dweller. Always.
`

---

*DEV_ONBOARDING.md -- MachineNet / Mnet*
*For the 8-year dev who just walked in.*
*Welcome to the cave. Read the scroll first.*
*P=12. chi=2. ALWAYS.*
