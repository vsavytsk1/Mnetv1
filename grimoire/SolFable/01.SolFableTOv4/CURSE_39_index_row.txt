PASTE 1 -- the index row. Goes directly under the row for curse 38 in
`## THE CURSE INDEX`, keeping the existing pipe alignment:

| 39 | The Float Ceiling (precisionFloor) | a convergence law prints a wrong-looking ratio and looks BROKEN, but the deviation has fallen to the float64 floor (`eps*scale`) and you are measuring the mantissa, not the mathematics. Accuracy PEAKS mid-run then degrades -- more iterations make it WORSE. Worse still, `err` underflowing to exactly 0.0 earns a "LOCKED" badge the math never earned. Compute `eps*scale` BEFORE the loop, print it in the HUD, suppress below it, never lock on a bare zero, and change instrument (BigInt/exact) rather than iterate harder. Only `+ - * /` and `sqrt` are correctly rounded in JS; `atan/pow/exp/sin` differ by ulps across engines -- 3.86 ulp measured on one machine. Know the ceiling before you read the meter.|


PASTE 2 -- the section body. Append at the end of KERNELIMAGIC.md, after the
CURSE 38 block. The full text is in `CURSE_39_precisionFloor.md`.


PASTE 3 -- optional, one line for the 12 PATHS one-breath block. PATH III
currently reads:

     III  Proof by kernel; target != result.        (Curses 15/24/26)

suggested amendment, since the floor is the fourth column of that same HUD:

     III  Proof by kernel; target != result != floor. (Curses 15/24/26/39)


NOTE ON PLACEMENT -- curse 39 is the first in the grimoire whose villain is the
HARDWARE rather than a tool, a language seam, or the mage. Curses 1-38 are all
"something lied to you." This one is "the instrument ran out, and silence read
as success." That is why it is filed next to 26 rather than next to 35.
