ffmpeg -y -framerate 30 -i f%04d.png -c:v libx264 -crf 18 -pix_fmt yuv420p flow.mp4

270 frames, 700x700, the fundamental triangle walked once.
FLOW.csv carries the symmetry score per frame -- it dips to zero at
each corner and nowhere else.
