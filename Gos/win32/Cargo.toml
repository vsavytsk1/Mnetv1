[package]
name = "gos_win32"
version = "0.1.0"
edition = "2021"
description = "Raw Win32 FFI, declared by hand. The ONLY unsafe in the project."
license = "MIT"

# Still none. Every symbol is an `extern "system"` declaration against the DLLs
# Windows already loads. Two binaries share this ONE file so that RUSTIUM's
# claim -- that this project contains exactly one unsafe module -- stays true.
[dependencies]
